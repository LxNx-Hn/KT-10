# v1.0.1 대비 변경 라인바이라인 요약

- 기준: `eb77e3fd321772f3aa360fef00b8d07f31863f93` (커밋 메시지 "v1.0.1" — Git 태그 아님)
- 대상: 현재 작업 트리 (최신 `origin/main` `5e201c0c` + 미커밋 작업분)
- 총계: **64개 파일, +5,919 / −748** (`git diff eb77e3f --stat`)
- 생성시각: 2026-07-27 KST

변경은 두 계층으로 나뉩니다. 라인 번호는 **현재 파일 기준**이며, 계층 A는 이미 커밋된 내역,
계층 B는 이번 작업의 미커밋 내역입니다.

| 계층 | 구간 | 파일 | 증감 | 상태 |
| --- | --- | ---: | --- | --- |
| **A** | `eb77e3f` → `5e201c0` (main 커밋분) | 50 | +4,029 / −310 | 커밋됨 |
| **B** | `5e201c0` → 작업 트리 (이번 작업) | 40 | +1,916 / −474 | **미커밋** |
| 중복 | 두 계층 모두에서 수정된 파일 | 24 | — | — |

---

# 계층 A — v1.0.1 → 최신 main (`5e201c0`, 이미 커밋됨)

주제: 부산 QGIS 90m DEM 도입, 경사 색상 2·5·8% 확립, shade 캐시·공유 context,
TMAP 캐시·계단 감지 강화, VWorld 메모리 캐시, 날씨 캐시.

## A-1. 부산 QGIS 90m DEM 파이프라인

### `ai/data/precomputed/busan_dem_clipped_90m.tif` (신규, 337KB 바이너리)
QGIS로 사전 생성한 부산 전역 90m DEM. EPSG:5179, 해상도 90m.

### `ai/features/elevation.py` (+177 / −30)
| 라인(현재) | 변경 |
| --- | --- |
| 1–6 | 모듈 docstring: "운영 요청은 메모리에 적재한 부산 지역 DEM을 사용한다. Open-Meteo와 GLO-90 COG는 범위 밖 fallback"으로 재정의 |
| 22–26 | `numpy`, `rasterio`, `affine.Affine`, `rasterio.warp.transform` 신규 import |
| 37 | `REGIONAL_DEM_SOURCE = "Busan DEM 90m (QGIS precomputed)"` 상수 추가 |
| 38 | `CACHE_SCHEMA_VERSION` 2 → **3** |
| 44–45 | `_regional_dem_lock`, `_regional_dem` 전역 상태 추가 |
| 48–53 | `RegionalDem` NamedTuple(values/transform/crs/nodata) 신규 |
| 55–64 | `_regional_dem_path()` — `ELEVATION_REGIONAL_DEM_PATH` 또는 번들 기본 경로 |
| 67–106 | `prepare_regional_dem()` — DEM을 1회만 메모리 적재. **EPSG:5179 + 90m 해상도 검증 실패 시 ValueError**(형식 위장 방지) |
| 109–113 | `regional_dem_ready()` — readiness 진단용 |
| 116–161 | `_regional_dem_elevations()` — 좌표 변환 후 **이중선형 보간**. nodata·비유한값이면 `None` 반환(0 대체 금지) |
| 302–304 | `_local_dem_elevations()` 앞단에 지역 DEM 우선 조회 삽입 |

### `ai/main.py` (+22 / −7)
| 라인 | 변경 |
| --- | --- |
| 19 | `prepare_regional_dem`, `regional_dem_ready` import |
| 36–43 | lifespan에서 DEM 선적재, **없으면 `RuntimeError`로 기동 실패**(조용한 저하 금지) |
| 107–117 | readiness에 `dem_ready`, `exact_walk_geometry_ready`를 **ready 판정에 포함**(이전엔 capability 표시만) |
| 122–129 | checks에 `regional_dem_precomputed`, `exact_walking_geometry_ready` 추가 |

### `ai/features/route_feature_cache.py` (신규, +134)
OD별 정적 후보·경사 피처 파일 캐시. `cache_identity()`가 geometry profile(loadLane 여부,
TMAP 설정, OSMnx, DEM 경로)까지 키에 포함. schema v2, TTL 30분, 원자적 tmp→replace 쓰기.

### `ai/api/router.py` (+220 / −13)
| 라인 | 변경 |
| --- | --- |
| 27–32 | route feature cache import |
| 105 | `max_walk_distance_m` 상한 10,000 → **15,000** |
| 106 | `candidate_limit: int = Field(default=5, ge=1, le=10)` 요청 필드 신규 |
| 461–503 | `_collect_featured_routes` → `_collect_static_featured_routes`로 분리(정적 수집) |
| 505–523 | `_limit_cached_route_features()` — 더 큰 캐시에서 요청 후보 수만 선택 |
| 526–539 | `_apply_request_features()` — 캐시된 정적 피처에 요청별 날씨·조건만 재결합 |
| 552–598 | 새 `_collect_featured_routes()` — 캐시 조회 → request lock → 재조회 → 수집 → 조건부 저장 |

### `ai/Dockerfile` (+1) / `backend/Dockerfile` (+2/−1)
`route_features`, `shade` 캐시 디렉터리 생성 추가.

## A-2. 그늘 성능·정확도

### `backend/app/shade_cache.py` (신규, +160)
경로 ID × 30분 departure bucket 단위 영구 캐시. schema v4.
`estimated_public` + `shade_ratio` 있는 성공만 저장(오류·미확정 캐시 금지).
`get_or_compute()`가 동일 경로·시각 동시 요청을 lock으로 1회만 계산.

### `backend/app/shade.py` (+294 / −57)
| 라인 | 변경 |
| --- | --- |
| 26–27 | `DISPLAY_SIMPLIFY_TOLERANCE_M = 0.5`, `DISPLAY_COORDINATE_DECIMALS = 7` |
| 89–111 | `_project_mapping()` — 건물 footprint 좌표를 **검증하며** 투영(비객체·비숫자·범위밖 ValueError) |
| 121–132 | `_unproject_display()` — 표시용 좌표 7자리 반올림 |
| 240–256 | `_PreparedBuildingShadow`, `PreparedShadeContext` dataclass 신규 |
| 259–348 | `_shadow_polygons` → `_prepare_shadow_context`로 분해. **건물 5개 초과 시 경로 bounds 기반 사전 필터**로 불필요한 그림자 생성 제거 |
| 351–368 | `_shadow_geometry_for_route()` — 준비된 그림자에서 경로 근접분만 union |
| 394–435 | `prepare_shade_context()` — **후보군 전체가 태양 위치·건물 그림자를 1회만 공유** |
| 438–454 | `_display_polygons()` — 전송 전 폴리곤 단순화(정확도 유지, payload 축소) |
| 463–468 | `calculate_shade(..., prepared_context=None)` 파라미터 추가 |
| 680–700 | 표시 폴리곤에 `_unproject_display` 적용 |

### `backend/app/main.py` (계층 A분: +428 / −73 중)
| 라인 | 변경 |
| --- | --- |
| 108–113 | `_effective_departure()` — 출발시각 KST 정규화 헬퍼 |
| (구) `_has_reusable_shade()` | 같은 시각·품질의 기존 shade 재사용 판정 신규 |
| 175–198 | VWorld 경로에서 **디스크 shade 캐시 먼저 조회** 후 `pending`만 계산 |
| 217–244 | `prepare_shade_context` + `get_or_compute_shade` 조합으로 후보별 병렬 계산 |
| 346–361 | `_filter_viable_candidates` 재작성: "대중교통 있으면 3km 초과 도보 제외" → **모든 경로에 `max_supported_total_walk_m`(15km) 동일 적용, 전부 탈락 시 422** |
| 378–384, 417–427 | `wait_for_buildings=True`, `candidate_limit=req.top_n` 전달 추가 |

### `backend/app/providers/vworld_buildings.py` (+124 / −9)
| 라인 | 변경 |
| --- | --- |
| 30–31 | `MAX_MEMORY_CACHE_BOXES = 4096`, `MAX_MEMORY_ROUTE_BUILDING_SETS = 1024` |
| 36–48 | 박스별·경로별 메모리 캐시 dict와 guard 추가 |
| 51–56 | `_cache_namespace()` — 키·도메인·캐시경로 해시로 캐시 오염 방지 |
| 59–98 | `_store_memory_cache` / `_read_route_memory_cache` / `_store_route_memory_cache` |
| 251–260 | `_read_cached_features()`가 **메모리 캐시 우선** 조회 |
| 이하 | corridor 조회 결과 메모리 캐시 저장, 중복 feature ID 검증 강화 |

### `backend/app/providers/weather.py` (+71)
| 라인 | 변경 |
| --- | --- |
| 24–27 | `_weather_cache`, guard, 루프별 `_request_locks` 추가 |
| 30–35 | `_weather_cache_key()` — 키+중심좌표 해시 |
| 38–70 | `_read_weather_cache` / `_store_weather_cache` / `_clear_weather_cache` |
| 147–156 | `get_current_weather()`가 **TTL 캐시 + single-flight lock** 경유 (성공 응답만 캐시) |

## A-3. TMAP 수집기 강화

### `ai/collectors/tmap_collector.py` (+258 / −78)
| 라인 | 변경 |
| --- | --- |
| 8–19 | asyncio/json/logging/time/hashlib/Path/Lock/uuid/WeakKeyDictionary import |
| 24–34 | `CACHE_SCHEMA_VERSION = 1`, `QUOTA_BACKOFF_SECONDS = 60.0`, 캐시·lock·backoff 전역 |
| 37–52 | `_cache_identity()` — OD 좌표 7자리 |
| 55–83 | `_cache_path` / `_read_cache` — TTL·schema 검증 |
| 91–101 | `_request_lock()` (OD별 single-flight), `_request_semaphore()` (`TMAP_MAX_CONCURRENT_REQUESTS`) |
| 104–126 | `_quota_backoff_active` / `_start_quota_backoff` / `_clear_quota_backoff` — **쿼터 초과 시 60초 백오프**(재시도 폭주 방지) |
| 129–152 | `_write_cache()` 원자적 저장 |
| 170–299 | `_candidate_from_data()` 분리 + 중첩 필드 타입 검증 강화, 계단 facilityType(1·11·"계단") 감지 |

## A-4. 지도 표시 (경사 기준 확립)

### `frontend/src/v2/KakaoMap.tsx` (+14 / −22)
| 라인 | 변경 |
| --- | --- |
| 142–148 | 주석 경사 등급 3/6/10% → **2/5/8%** |
| 150–155 | `SLOPE_COLOR_RAMP` 값 교체: `#16a34a/#ca8a04/#ea580c/#dc2626`(3·6·10) → **`#2ca25f/#f2cf4a/#f28e2b/#d73027`(2·5·8)**, 라벨 "위험" → "매우 급경사" |
| 461 | 선택 경로 외곽선 zIndex 3 → **4** |
| 470 | 선택 경로 본선 zIndex 4 → **5** |
| 525 | 그늘 경로 강조선 zIndex 5 → **3** (경사선이 그늘 위로 오도록 순서 확정) |

### `frontend/src/v2/map-first.css` (+4 / −4)
`--slope-flat/moderate/steep/danger` 범례 점 색상을 지도 램프와 동일 값으로 교체.

## A-5. 기타 (계층 A)

| 파일 | 변경 |
| --- | --- |
| `backend/app/api/feedback.py:62` | `max_walk_distance_m` 상한 10,000 → 15,000 |
| `frontend/src/components/ProfilePreferences.tsx:60` | 입력 `max` 10000 → 15000 |
| `scripts/benchmark_route_candidate_counts.py` (신규 +308) | 후보 수별 cold/warm 벤치마크 스크립트 |
| `docs/performance/route-candidate-benchmark-2026-07-26.json` (신규 +1,205) | 2026-07-26 측정 결과 |
| `backend/tests/test_shade_cache.py` (신규 +111), `test_providers.py` (+51), `test_shade.py` (+94), `ai/tests/test_route_feature_cache.py` (신규 +50) | 위 기능 회귀 테스트 |
| `README.md`, `docs/DATA.md`, `DEPLOYMENT.md`, `LOCAL_TESTING.md`, `PLAN.md`, `PRODUCT_DECISIONS.md`, `IMPLEMENTATION.md`, `ai/README.md`, `frontend/src/v2/README.md` | DEM·캐시·경사 기준 문서 갱신 |

---

# 계층 B — 최신 main → 현재 작업 트리 (이번 작업, 미커밋)

주제: ODsay 지연 정밀화(호출 7→2회), 동적 후보 수 단일 권위, shade gate,
경사 network fallback 격리, 그늘 토글 제거.

## B-1. ODsay 계측·동시성 (신규 모듈)

### `ai/collectors/odsay_instrumentation.py` (신규, +285)
| 라인 | 내용 |
| --- | --- |
| 27–28 | `KST`, `_BUDGET_WARN_RATIOS = (0.7, 0.8, 0.9, 1.0)` |
| 31–41 | `correlation_id` ContextVar, `new_correlation_id()`, `anonymized_hash()` (SHA-256 12자) |
| 44–106 | `OdsayCallCounters` — 논리 호출·network 호출·cache hit·follower·retry·semaphore 대기 집계 |
| 111–147 | `log_call()` — 구조화 로그. **키·좌표·mapObj 원문 미기록** |
| 152–214 | `record_network_call()` — 날짜별 원자적 counter 파일. 저장 실패는 경로 결과에 영향 없이 경고만 |
| 216–225 | 70/80/90/100% 예산 경고(1회성) |
| 228–239 | `read_daily_counter()` — `observed_service_calls_today`/`estimated_remaining_service_budget` |
| 244–262 | `with_concurrency_limit()` — 루프별 semaphore(`ODSAY_MAX_CONCURRENT_REQUESTS`)와 대기시간 계측 |
| 275–285 | `single_flight()` — leader를 **독립 Task + `asyncio.shield`**로 실행. follower 취소가 leader를 죽이지 않고, 완료 콜백이 in-flight 제거, 오류는 전 대기자에 전파 |

### `ai/config.py` (+30 / −2)
| 라인 | 변경 |
| --- | --- |
| 29–31 | `ODSAY_MAX_CANDIDATES` 주석을 "조용히 자르지 않고 명시적 오류"로 변경 |
| 32–37 | `ODSAY_MAX_CONCURRENT_REQUESTS: int = 3` (1~10), `ODSAY_DAILY_BUDGET: int = 1000` 신규 |
| 46–50 | `ELEVATION_NETWORK_FALLBACK_ENABLED: bool = False` 신규 — **live에서 원격 DEM·Open-Meteo 차단** |

## B-2. ODsay 수집기 — 지연 정밀화

### `ai/collectors/odsay_collector.py` (+305 / −105)
| 라인 | 변경 |
| --- | --- |
| 23–32 | instrumentation 모듈 import |
| 237–347 | `_load_lane()` 재작성: `client` 인자 제거(자체 client 생성), **single-flight + semaphore + 캐시 재확인 + 계측** 추가. `finally`에서 counter·로그 기록. **검증 성공 후에만 캐시 저장**(오류·빈 응답 미저장) |
| 349–377 | **`refine_transit()` 신규** — 선택 후보 1건의 loadLane만 조회하는 공개 진입점. search·재수집 없음 |
| 484–526 | `_build_candidate()` 시그니처에서 `client` 제거, docstring에 지연 정밀화 계약 명시 |
| 519–533 | loadLane 즉시 호출 제거 → mapObj 형식만 **수집 시점에 검증**하고 `refinement` 서술자(provider/map_object/origin/destination) 구성 |
| 534–538 | 모든 대중교통 구간을 정류장 관측 좌표 기반 `_estimated_transit_path()`로 조립 |
| 596–598 | 대중교통 구간 품질을 **무조건 `"estimated"`** 로 기록 (exact 위장 금지) |
| 620 | `RouteCandidate(..., transit_refinement=refinement)` 전달 |
| 630–730 | search 호출에 single-flight·semaphore·계측·`record_network_call` 적용, `finally` 로깅 |
| 733–768 | 후보 배치 조립에서 loadLane 제거(보행 geometry 병렬만 유지) |
| 796–802 | `min(max_candidates, MAX)` **silent clamp 삭제** → 초과 시 `CollectorError` |
| 803–809 | 상한 초과 명시적 오류 메시지 + `new_correlation_id()` |

### `ai/collectors/base.py` (+3)
`RouteCandidate`에 `transit_refinement: Optional[dict] = None` 필드 추가(서버 내부 전용 주석).

### `ai/merger/route_merger.py` (+10)
| 라인 | 변경 |
| --- | --- |
| 27–28 | `MergedRoute.transit_refinement` 필드 추가 |
| 201–214 | 병합 시 대표 후보의 refinement 서술자 승계(품질 우선 교체 + None이면 보충) |
| 224 | 신규 MergedRoute 생성 시 서술자 전달 |

### `ai/features/route_feature_cache.py` (+4 / −1)
`CACHE_SCHEMA_VERSION` 2 → **3** (지연 정밀화·semantic route ID 도입, 이전 캐시는 miss 처리).

## B-3. AI API — semantic route ID·refinement endpoint

### `ai/api/router.py` (+124 / −18)
| 라인 | 변경 |
| --- | --- |
| 18–23 | `CollectorError`, `CollectorNotConfigured` import 추가 |
| 354–362 | **`RefineTransitRequest` 모델 신규** (origin/dest 좌표 + map_object) |
| 364–390 | **`POST /routes/refine-transit` 엔드포인트 신규** — loadLane만 수행, 미설정 503 / 공급자 실패 502(가짜 geometry 금지) |
| 406–417 | `labeling_candidates`에 `candidate_limit > ODSAY_MAX_CANDIDATES` **422** 검증 추가 |
| 449 | 후보 응답에 `transit_refinement` 서술자 포함(백엔드 내부 전용) |
| 561–563 | feature dict에 `_transit_refinement` 키 저장 |
| 596–607 | **`_walk_geometry_exact()` 신규** — walk/transfer 구간 전부 exact인지 판정 |
| 609–624 | `_static_features_cacheable()` 완화: 전체 `exact` **또는 보행만 exact**면 캐시 허용(대중교통 estimated는 차단 사유 아님) |
| 830–866 | **`_route_id()` 재작성** — 전체 정밀 path 해시 → **semantic fingerprint**(보행 path 41점 샘플 + mode/노선명/역명/description/distance). 정밀화 전후 ID 불변의 근거 |

## B-4. Backend — 후보 수 권위·refinement·shade gate

### `backend/app/settings.py` (+15)
| 라인 | 변경 |
| --- | --- |
| 66–68 | `route_default_top_n: int = Field(default=5, ge=1, le=10)` 신규 — **운영 기본 후보 수 단일 권위** |

### `backend/app/models.py` (+28 / −2)
| 라인 | 변경 |
| --- | --- |
| 227–234 | `RouteCandidate`에 `transit_refinement`(`exclude=True`, mapObj 비노출), `transit_refinement_state`(not_loaded/loading/exact/failed), `transit_refined_at` 추가 |
| 362, 369 | `RecommendRequest.top_n`, `ShadeRefreshRequest.top_n`을 `int = 5` → **`int | None = None`** (생략 시 서버 기본값 사용) |
| 372–377 | **`TransitRefineRequest` 신규** — routeSetToken + routeId만 |
| 379–387 | **`TransitRefinementResponse` 신규** — routeId/path/segments/geometryQuality/refinedAt만(score·rank 미포함) |

### `backend/app/route_set_cache.py` (+110 / −5, 전면 재작성)
| 라인 | 변경 |
| --- | --- |
| 17 | `ROUTE_SET_SCHEMA_VERSION = 2` |
| 21–29 | `CachedRouteSet`에 `metadata`, `revision` 필드 추가 |
| 32–33 | **`StaleRouteSetRevision`** 예외 신규 |
| 55–57 | 토큰별 `asyncio.Lock` 저장소 추가 |
| 83–90 | **`token_lock()`** — refinement/refresh read-modify-write 직렬화 |
| 92–120 | `put()`에 metadata·revision=1 기록, 만료·evict 시 lock도 정리 |
| 133–158 | **`replace(expected_revision=...)`** — revision 불일치 시 `StaleRouteSetRevision`(stale 덮어쓰기 차단) |
| 160–199 | **`update_candidate()` 신규** — 특정 route ID 후보만 원자적 수정, 순서·다른 후보·weather 불변, **TTL 미연장** |

### `backend/app/providers/ai_pipeline.py` (+171 / −3)
| 라인 | 변경 |
| --- | --- |
| 11 | `ZoneInfo` import |
| 275–294 | shade 평가시각 계약 완화: 후보 전원 shade 필수 → **계산된 것끼리만 시각 일치 요구, 전부 없으면 출발시각 기준 사용**(§24) |
| 316–322 | sources 구성 시 `route.shade` None 허용 |
| 736–746 | `_to_route_candidate`에서 `transit_refinement` 검증 + `refinement_state` 판정(exact/not_loaded/failed) |
| 771–773 | RouteCandidate에 refinement 필드 전달 |
| 782–802 | **`_valid_refinement_descriptor()`** — 서술자 형식 검증 |
| 804–857 | **`apply_refined_transit_geometry()`** — 대중교통 구간에만 정밀 선형 적용, 전체 path 재조립, `geometry_quality` 재계산. **score·rank·snapshot·feedback token·terrain·shade 미변경**. lane 수 불일치 시 502 |
| 859–915 | **`refine_candidate_transit()`** — 이미 exact면 외부 호출 0회, AI `/routes/refine-transit` 호출, 응답 검증 실패 시 502(가짜 성공 금지) |

### `backend/app/providers/__init__.py` (+1)
`refine_candidate_transit` re-export.

### `backend/app/shade.py` (계층 B분)
| 라인 | 변경 |
| --- | --- |
| 371–381 | **`building_height_counts()` 신규** — 그림자 계산 없이 (높이확인, 전체) 건물 수만 집계 |
| 597–636 | `calculate_shade`에 **높이 완전성 gate** 삽입: `data_quality == "public"` 이고 coverage < 100%면 그림자 폴리곤·경로 교차 계산 없이 `unavailable` 반환. **`lower_bound` 표시 폐지** |

### `backend/app/main.py` (계층 B분)
| 라인 | 변경 |
| --- | --- |
| 40–41, 48, 57 | `TransitRefineRequest`, `TransitRefinementResponse`, `refine_candidate_transit`, `StaleRouteSetRevision`, `building_height_counts` import |
| 119–121 | **`_effective_top_n()`** — 요청 topN 없을 때만 `route_default_top_n` 적용 |
| 124–166 | **`_shade_gate_reason()` 신규** — 저비용 순서로 판정: ①10~18시 KST ②weather 존재 ③observed_at/timezone 유효 ④관측 age ≤ TTL ⑤**출발시각이 관측 유효기간 이내**(미래 예보 오용 금지) ⑥체감 25°C 이상 |
| 169–175 | `_add_configured_shade(..., weather=None)` 파라미터 추가 |
| 226–235 | **gate 미충족 시 `candidate.shade = None` 후 즉시 반환 — VWorld 0회, 그림자 0회** |
| 277–290 | 건물 높이 coverage < 100%면 `prepare_shade_context` 자체를 생략 |
| 315–338 | `_cache_scored_routes`에 `expected_revision`, `metadata` 파라미터 추가 |
| 342–354 | **`_route_set_metadata()`** — originalRequestedTopN/effectiveTopN/collectedCandidateCount/기본값 기록 |
| 356–375 | **`_refine_top_ranked_transit()`** — 순위 확정 후 1위만 정밀화. 실패 시 **정책 B**(estimated 부분 성공, 로그·상태만 기록) |
| 458–459, 486, 495, 507 | ai 모드: `candidate_limit=effective_top_n`, `weather=` 전달, 1위 정밀화 호출, metadata 저장 |
| 546, 561, 585, 594–603 | live/demo 모드에 동일 적용(`select_representative_routes(scored, effective_top_n)`) |
| 621–711 | `routes_refresh_shade` 재작성: **token lock 감싸기**, 저장된 effectiveTopN 우선, **수집 수 초과 topN은 409**, `expected_revision` CAS, `StaleRouteSetRevision` → 409 |
| 750–828 | **`POST /api/routes/refine-transit` 신규** — token/route ID 검증, 이미 exact면 캐시 반환(외부 호출 0), 실패 시 상태 `failed` 기록 후 원본 status 전파, 성공 시 `update_candidate`로 해당 후보만 patch |
| 851, 856 | labeling endpoint에도 `_effective_top_n`, `weather=` 적용 |

## B-5. 경사 network fallback 격리

### `ai/features/elevation.py` (계층 B분)
| 라인 | 변경 |
| --- | --- |
| 263–265 | `_ensure_dem_tile()`에서 캐시 없는 타일의 **원격 COG 다운로드 전 `ELEVATION_NETWORK_FALLBACK_ENABLED` 검사**(기본 false → 다운로드 안 함) |
| 570–573 | Open-Meteo 호출 직전 동일 플래그 검사 → 미허용 시 `_empty("unavailable")`. **누락 고도를 0·합성값으로 대체하지 않음** |

## B-6. Frontend

### `frontend/src/types/index.ts` (+9)
`TransitRefinement` 인터페이스 신규(routeId/path/segments/geometryQuality/refinedAt).

### `frontend/src/adapters/types.ts` (+10)
`RouteAdapter.refineTransit(routeSetToken, routeId)` 계약 추가(미지원 모드는 `null`).

### `frontend/src/adapters/live.ts` (+18 / −5)
| 라인 | 변경 |
| --- | --- |
| 95–103 | `recommend`의 `topN = 5` **기본값 제거** → 미지정 시 body에서 생략(서버 기본값 사용) |
| 104–115 | `refreshShade` 동일 처리 |
| 116–120 | **`refineTransit()` 신규** — `/api/routes/refine-transit`에 토큰+routeId만 전송 |

### `frontend/src/adapters/mock.ts` (+2)
`refineTransit: () => Promise.resolve(null)` (고정 데모는 이미 확정 geometry).

### `frontend/src/store/appStore.ts` (+67 / −57)
| 라인 | 변경 |
| --- | --- |
| 43 | `transitRefinementInFlight = new Set<string>()` — 후보별 중복 요청 차단 |
| 45–52 | `needsTransitRefinement()` — bus/subway 구간이 exact가 아닌 후보만 대상 |
| 118–121 | 인터페이스에서 `refreshEnrichment` 제거 |
| 267–316 | **`selectRoute` 재작성** — 선택 즉시 반영 후, 필요 시 refinement 호출. **응답 route ID 일치 후보의 저장 geometry만 patch**(늦은 응답이 현재 지도 미변경), 실패 시 estimated 유지 |
| (삭제) | `refreshEnrichment()` 전체 제거 — 전체 `/recommend` 재실행 경로 소멸 |

### `frontend/src/v2/MapFirstApp.tsx` (+18 / −72)
| 라인 | 변경 |
| --- | --- |
| 34 | `SLOPE_COLOR_RAMP` import 추가 |
| (삭제) | `ShadeIcon` 컴포넌트, `showShade` state, `refreshEnrichment` 구독 |
| (삭제) 1049–1083 | **5/10/20/30초 enrichment 재추천 타이머 useEffect 제거** |
| 1188 | `<KakaoMap>`에서 `showShade` prop 제거 |
| (삭제) 1414–1429 | **그늘 토글 FAB 버튼 제거**(aria-pressed 포함) |
| 1382–1383 | 그늘 범례 조건에서 `showShade &&` 제거 → shade 있으면 자동 표시 |
| 1396–1410 | **경사 범례를 `SLOPE_COLOR_RAMP` 순회로 생성** — 하드코딩 3/6/10% 4줄 삭제, 지도와 단일 상수 공유 |

### `frontend/src/v2/KakaoMap.tsx` (계층 B분)
| 라인 | 변경 |
| --- | --- |
| 26–34 | `KakaoMapProps`에서 `showShade` 제거 |
| 340–356 | `propsRef`에서 `showShade` 제거 |
| 480–486 | `addShadeOverlay(maps, route, boundsPoints)` — `visible` 인자 삭제, **shade 있으면 자동 표시** |
| 596–615 | `renderMapData` 시그니처·호출부에서 `shadeVisible` 제거 |
| 723, 762–781 | mount/data effect 의존성에서 `showShade` 제거 |

### `frontend/src/components/MapView.tsx` (+7 / −29, 레거시 화면)
`showShade` state·토글 버튼·분기 전부 제거, `ShadeControls`는 비율·범례 표시만 담당.

## B-7. 설정·문서

| 파일 | 변경 |
| --- | --- |
| `.env.production.example` | `ODSAY_MAX_CONCURRENT_REQUESTS=3`, `ODSAY_DAILY_BUDGET=1000`, `ROUTE_DEFAULT_TOP_N=5` 추가 |
| `ai/.env.example` | 위 ODsay 2개 + `ELEVATION_NETWORK_FALLBACK_ENABLED=false` 추가 |
| `backend/.env.example` | `ROUTE_DEFAULT_TOP_N=5` 추가 |
| `docker-compose.yml` / `.prod.yml` | 동일 환경변수 3~4개 전달 추가 |
| `scripts/benchmark_route_candidate_counts.py:1,213` | 기본 후보 수 `3,5,10` → **`3,5,7,10`** |
| `docs/IMPLEMENTATION.md:18-31` | 4번 항목을 지연 정밀화 흐름으로 재작성, 5번에 shade gate 6조건 명시 |

## B-8. 테스트

| 파일 | 변경 |
| --- | --- |
| `ai/tests/test_odsay_lazy_refinement.py` (신규 +246) | search만 호출 / 동시 10요청 single-flight / semaphore 상한 / 상한 초과 오류 / leader 오류 전파 / 일일 counter·경고 |
| `backend/tests/test_transit_refinement.py` (신규 +433) | topN 권위 3종, refinement 409·422·캐시 재사용·단일 후보 patch·실패 상태, snapshot 불변, revision CAS, 동시 refinement, shade gate 7종 |
| `ai/tests/test_collectors.py` (+180/−34) | 기존 "search+loadLane" 테스트를 **"search만 + 서술자 보존"** 으로 교체, `refine_transit` 테스트 추가, `_build_candidate` 시그니처 반영 |
| `ai/tests/test_api.py` (+204/−4) | 캐시 가능 판정에 대중교통-estimated 케이스 추가, 422 상한, refine endpoint 2종, **route ID 정밀화 전후 불변·노선/정류장 구분** |
| `ai/tests/test_elevation.py` (+142/−2) | 4개 테스트에 fallback 플래그 명시, **기본 설정에서 network fallback 0회** 테스트 추가 |
| `backend/tests/test_api.py` (+211/−5) | 시각 고정 헬퍼(`_GateFixedDatetime`)·유효 날씨 헬퍼 추가, gate 반영해 3개 테스트 수정, 야간 shade None 계약 |
| `backend/tests/test_vworld_buildings.py` (+119/−6) | 높이 불완전 → `unavailable` 로 기대값 변경, **높이 완전 시 비율 계산** 테스트 신규 |
| `frontend/src/store/appStore.enrichment.test.ts` (+203/−35) | `refreshEnrichment` 테스트 → **카드 선택 정밀화 4종**(patch, exact 재선택 0호출, 늦은 응답 무시, 중복 선택 단일화) |
| `frontend/src/adapters/live.test.ts` (+56/−1) | topN 미전송 검증, refineTransit 페이로드에 mapObj 미포함 검증 |
| `frontend/src/__tests__/ui.test.tsx` (+45/−17) | 토글 mock 제거, **자동 표시 / 결과 없으면 생략 / 범례 2·5·8% 일치** 3종 |
| `frontend/src/v2/KakaoMap.test.tsx` (+79/−20) | 모든 `showShade` prop 제거, shade 제거 시나리오를 "결과 없음"으로 전환 |

---

## 검증 결과

| 항목 | 결과 |
| --- | --- |
| AI pytest | 197 passed, 2 skipped |
| Backend pytest | 231 passed, 1 skipped |
| Frontend vitest | 115 passed |
| TypeScript / PWA build | pass |
| ruff (변경 파일) | 신규 지적 0 |
| pip check / pip-audit / npm audit | 0 |

## 핵심 동작 변화 요약

| 항목 | v1.0.1 | 현재 |
| --- | --- | --- |
| 후보 5개 최초 ODsay 호출 | 최대 7회 (search 1 + loadLane 6, 배치 overfetch) | **2회** (search 1 + 1위 loadLane 1) |
| 2위 이하 대중교통 선형 | 최초부터 exact | estimated → **카드 선택 시 1회 정밀화**, 재선택 0회 |
| route ID | 전체 정밀 path 해시 | **semantic fingerprint**(정밀화 전후 불변) |
| 기본 후보 수 | frontend·backend 각각 5 하드코딩 | **`ROUTE_DEFAULT_TOP_N`** 단일 권위, 초과 요청 422 |
| 경사 등급 | 지도 2·5·8% / 범례 3·6·10% (불일치) | **단일 상수 공유(2·5·8%)** |
| 경사 live fallback | 원격 COG·Open-Meteo 호출 가능 | 기본 차단(`ELEVATION_NETWORK_FALLBACK_ENABLED=false`) |
| 그늘 조건 | 태양 고도 + exact 보행 | **+ 10~18시 / 유효 관측 / 체감 25°C / 높이 100%** |
| 그늘 UI | 토글 버튼(aria-pressed) | 토글 없음, 결과 있으면 자동 표시 |
| enrichment 재추천 | 5/10/20/30초 전체 `/recommend` 재실행 | 제거 |
