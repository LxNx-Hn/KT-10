# 단일 배포 환경파일 예시.
# `python scripts/prepare_deployment_env.py --import-existing` 실행 후
# 외부 서비스 키만 채우고 `--check`로 검증하세요.

PUBLIC_ORIGIN=https://your-domain.example
PORT=8080

# 운영 공급자 선택
ROUTE_MODE=live
BUILDING_SOURCE=vworld

# 외부 서비스 키
# JavaScript 키는 브라우저 지도/장소 SDK용이고 REST 키는 서버 장소검색·로그인용입니다.
# 서로 대체할 수 없습니다.
VITE_KAKAO_MAP_KEY=
KAKAO_REST_API_KEY=
KAKAO_OAUTH_CLIENT_SECRET=
ODSAY_API_KEY=
TMAP_API_KEY=
# 느린 OSM 보행망 복구를 명시적으로 허용할 때만 true
OSMNX_WALK_GEOMETRY_ENABLED=false
# human_validated(기본) 또는 명시적 데모용 judge_baseline
RANKER_TIER=human_validated
VWORLD_API_KEY=
OPENWEATHER_API_KEY=
BUS_SERVICE_KEY=

# 아래 4개는 준비 스크립트가 안전한 난수로 자동 생성합니다.
POSTGRES_PASSWORD=
SESSION_SECRET=
TRAINING_ANONYMIZATION_SALT=
LABELING_API_TOKEN=

# 필요할 때만 조정
REQUEST_TIMEOUT=8
