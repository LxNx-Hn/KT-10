# Claude Code 작업 지시서 — AI팀 전체 파이프라인

> 이 문서를 전부 읽고 구조를 완전히 파악한 뒤 순서대로 작업하세요.
> 작업 전 현재 레포 전체를 스캔하여 기존 파일 구조를 먼저 파악하세요.

---

## 0. 절대 원칙 (모든 작업에서 반드시 준수)

### ① 상위 폴더 구조 고정 — 절대 변경 금지

현재 레포의 상위 폴더는 아래 5개로 고정합니다.

```
backend/
frontend/
data/
docs/
ai/
```

- 이 5개 외 상위 폴더를 절대 새로 생성하지 마세요
- pathfinding/ 폴더가 상위에 있다면 ai/ 하위로 이동하거나 삭제하세요
- 새 작업은 반드시 위 5개 중 하나의 하위 폴더 또는 기존 파일에 추가하는 방식으로 처리

### ② 길찾기 알고리즘 코드 전부 삭제

- KSP, ACO, GA, Tree-based Search 등 직접 구현한 알고리즘 코드 전부 삭제
- pathfinding/ 폴더가 있다면 전체 삭제 (내용 확인 후 ai/ 하위로 이동 필요한 것만 이동)
- 경로 수집은 ODsay API로 대체 (섹션 3 참고)

### ③ data/ 폴더 구조 — 열면 바로 파일이 보여야 함

```
data/
├── raw/          # 데이터팀 제공 원본 CSV / XLSX 파일
├── cache/        # GeoDataFrame pickle 캐시 (.gitignore 처리)
└── processed/    # 전처리 완료 파일 (필요 시)
```

- 현재 data/ 에 흩어져 있는 CSV/XLSX 원본 파일들을 data/raw/ 로 이동
- data/cache/ 는 .gitignore 에 추가

---

## 1. 현재 레포 구조 파악 (작업 시작 전 필수)

작업 전 아래 파일들을 전부 읽고 파악하세요.

```
backend/app/main.py
backend/app/routers/
backend/app/services/
frontend/src/
ai/
data/
```

기존 ai/ 내 코드와 backend/ 라우터를 파악한 뒤,
아래 작업 내용을 기존 파일에 통합하거나 하위 폴더로 추가하세요.

---

## 2. 최종 폴더 구조

```
backend/
├── app/
│   ├── main.py
│   ├── config.py
│   ├── database.py
│   ├── models/
│   ├── schemas/
│   ├── routers/
│   │   ├── route.py
│   │   ├── place.py
│   │   └── health.py
│   └── services/
│       ├── route_service.py
│       └── place_service.py

frontend/
│   (기존 구조 유지 — 변경 없음)

data/
├── raw/
├── cache/
└── processed/

docs/
│   (기존 구조 유지 — 변경 없음)

ai/
├── preprocessing/
│   ├── __init__.py
│   ├── load_layers.py
│   └── parse_facilities.py
├── features/
│   ├── __init__.py
│   └── extractor.py
├── scoring/
│   ├── __init__.py
│   ├── train.py
│   ├── predict.py
│   └── explain.py
├── collectors/
│   ├── __init__.py
│   ├── base.py
│   ├── odsay_collector.py
│   ├── tmap_collector.py
│   └── osmnx_collector.py
├── merger/
│   ├── __init__.py
│   └── route_merger.py
├── rl/
│   ├── __init__.py
│   ├── environment.py
│   ├── agent.py
│   └── reward.py
├── api/
│   ├── __init__.py
│   └── router.py
├── tests/
│   ├── __init__.py
│   ├── test_load_layers.py
│   ├── test_extractor.py
│   ├── test_scoring.py
│   └── test_collectors.py
└── requirements.txt
```

---

## 3. ODsay API 기반 경로 수집

### 3-1. 기존 코드 삭제

- pathfinding/ 폴더 전체 삭제
- KSP, ACO, GA, Tree 관련 코드 및 import 전체 제거

### 3-2. 수집기 베이스 클래스 (ai/collectors/base.py)

```python
"""경로 수집기 공통 인터페이스."""
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from typing import Optional


@dataclass
class Coordinate:
    lat: float
    lng: float


@dataclass
class RouteCandidate:
    source: str
    path: list
    duration_min: float
    distance_m: float
    raw_response: Optional[dict] = field(default=None)


class BaseRouteCollector(ABC):
    source_name: str = "base"

    @abstractmethod
    async def collect(self, origin: Coordinate, destination: Coordinate) -> list:
        """경로 후보 수집. 실패 시 빈 리스트 반환 (예외 전파 금지)."""
        pass
```

### 3-3. ODsay 수집기 (ai/collectors/odsay_collector.py)

```python
"""
ODsay 대중교통 경로 수집기 (메인 소스).

ODsay Lab API로 대중교통 경로 후보를 수집한다.
버스·지하철 환승 정보, 정류장 좌표를 세그먼트 단위로 제공.
API 키 미설정 시 플레이스홀더 반환.

API 문서: https://lab.odsay.com/guide/service
"""
import httpx
from ai.collectors.base import BaseRouteCollector, RouteCandidate, Coordinate
from ai.config import settings


class OdsayRouteCollector(BaseRouteCollector):
    source_name = "odsay"
    BASE_URL = "https://api.odsay.com/v1/api/searchPubTransPathT"

    async def collect(self, origin: Coordinate, destination: Coordinate) -> list:
        if not settings.ODSAY_API_KEY or settings.ODSAY_API_KEY.startswith("YOUR_"):
            return [RouteCandidate(
                source=self.source_name,
                path=[origin, destination],
                duration_min=0, distance_m=0,
                raw_response={"note": "ODSAY_API_KEY not configured — placeholder"},
            )]

        try:
            async with httpx.AsyncClient() as client:
                resp = await client.get(self.BASE_URL, params={
                    "SX": origin.lng, "SY": origin.lat,
                    "EX": destination.lng, "EY": destination.lat,
                    "apiKey": settings.ODSAY_API_KEY,
                }, timeout=10.0)
            data = resp.json()

            candidates = []
            for path in data.get("result", {}).get("path", [])[:3]:
                coords = []
                for sub in path.get("subPath", []):
                    for st in sub.get("passStopList", {}).get("stations", []):
                        coords.append(Coordinate(lat=float(st["y"]), lng=float(st["x"])))
                if not coords:
                    coords = [origin, destination]
                candidates.append(RouteCandidate(
                    source=self.source_name, path=coords,
                    duration_min=path.get("info", {}).get("totalTime", 0),
                    distance_m=path.get("info", {}).get("totalDistance", 0),
                    raw_response=path,
                ))
            return candidates
        except Exception:
            return []
```

### 3-4. TMAP 수집기 (ai/collectors/tmap_collector.py)

```python
"""
TMAP 보행자 길찾기 수집기 (보조 소스).

보행자 특화 경로, 계단/엘리베이터 속성 세그먼트 단위 제공.
facilityType 필드: 계단/엘리베이터/에스컬레이터 직접 파악 가능.
API 키 미설정 시 플레이스홀더 반환.
"""
import httpx
from ai.collectors.base import BaseRouteCollector, RouteCandidate, Coordinate
from ai.config import settings


class TmapRouteCollector(BaseRouteCollector):
    source_name = "tmap"
    BASE_URL = "https://apis.openapi.sk.com/tmap/routes/pedestrian"

    async def collect(self, origin: Coordinate, destination: Coordinate) -> list:
        if not settings.TMAP_API_KEY or settings.TMAP_API_KEY.startswith("YOUR_"):
            return [RouteCandidate(
                source=self.source_name,
                path=[origin, destination],
                duration_min=0, distance_m=0,
                raw_response={"note": "TMAP_API_KEY not configured — placeholder"},
            )]

        try:
            async with httpx.AsyncClient() as client:
                resp = await client.post(self.BASE_URL, json={
                    "startX": origin.lng, "startY": origin.lat,
                    "endX": destination.lng, "endY": destination.lat,
                    "startName": "출발지", "endName": "도착지",
                }, headers={"appKey": settings.TMAP_API_KEY}, timeout=10.0)
            data = resp.json()

            coords = []
            for feat in data.get("features", []):
                geom = feat.get("geometry", {})
                if geom.get("type") == "LineString":
                    for lng, lat in geom.get("coordinates", []):
                        coords.append(Coordinate(lat=lat, lng=lng))

            if not coords:
                return []

            props = data.get("features", [{}])[0].get("properties", {})
            return [RouteCandidate(
                source=self.source_name, path=coords,
                duration_min=props.get("totalTime", 0) / 60,
                distance_m=props.get("totalDistance", 0),
                raw_response=data,
            )]
        except Exception:
            return []
```

### 3-5. OSMnx Fallback (ai/collectors/osmnx_collector.py)

```python
"""
OSMnx 직접 계산 수집기 (fallback).

외부 API 상태와 무관하게 항상 최소 1개 경로를 보장한다.
부산진구 보행자 도로망 그래프를 data/cache/ 에 캐싱하여 최초 1회만 다운로드.
"""
import osmnx as ox
import networkx as nx
from itertools import islice
from pathlib import Path
from ai.collectors.base import BaseRouteCollector, RouteCandidate, Coordinate

GRAPH_CACHE = Path("data/cache/busanjin_walk.graphml")
_graph = None


def _get_graph():
    global _graph
    if _graph is not None:
        return _graph
    if GRAPH_CACHE.exists():
        _graph = ox.load_graphml(GRAPH_CACHE)
    else:
        GRAPH_CACHE.parent.mkdir(parents=True, exist_ok=True)
        _graph = ox.graph_from_place("Busanjin-gu, Busan, South Korea", network_type="walk")
        ox.save_graphml(_graph, GRAPH_CACHE)
    return _graph


class OsmnxRouteCollector(BaseRouteCollector):
    source_name = "osmnx"

    async def collect(self, origin: Coordinate, destination: Coordinate) -> list:
        """항상 최소 1개 경로 반환. 그래프 실패 시 직선 경로 플레이스홀더."""
        try:
            G = _get_graph()
            o_node = ox.nearest_nodes(G, X=origin.lng,      Y=origin.lat)
            d_node = ox.nearest_nodes(G, X=destination.lng, Y=destination.lat)

            paths = list(islice(
                nx.shortest_simple_paths(G, o_node, d_node, weight="length"), 3
            ))
            candidates = []
            for path_nodes in paths:
                coords = [Coordinate(lat=G.nodes[n]["y"], lng=G.nodes[n]["x"]) for n in path_nodes]
                dist   = sum(G[path_nodes[i]][path_nodes[i+1]][0].get("length", 0)
                             for i in range(len(path_nodes)-1))
                candidates.append(RouteCandidate(
                    source=self.source_name, path=coords,
                    duration_min=dist / 67, distance_m=dist,
                ))
            return candidates
        except Exception:
            return [RouteCandidate(
                source=self.source_name,
                path=[origin, destination],
                duration_min=0, distance_m=0,
                raw_response={"note": "OSMnx fallback — placeholder"},
            )]
```

### 3-6. API 키 파일 생성

#### 루트 .env.example 생성 (없으면 새로 만들고, 있으면 아래 내용 추가)

```env
# ============================================================
# 지도 API 키
# ============================================================

# 카카오맵 (지도 렌더링 + 장소 검색 + 주소-좌표 변환)
# 발급: https://developers.kakao.com → 앱 생성 → 플랫폼 키
VITE_KAKAO_MAP_KEY=YOUR_KAKAO_MAP_APP_KEY_HERE
KAKAO_REST_API_KEY=YOUR_KAKAO_REST_API_KEY_HERE

# Google Maps (비교 검토용 — 필수 아님)
# 발급: https://console.cloud.google.com → Maps JavaScript API
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# 네이버 지도 (비교 검토용 — 필수 아님)
# 발급: https://www.ncloud.com → AI·NAVER API → Maps
NAVER_CLIENT_ID=YOUR_NAVER_CLIENT_ID_HERE
NAVER_CLIENT_SECRET=YOUR_NAVER_CLIENT_SECRET_HERE

# ============================================================
# 경로 수집 API 키
# ============================================================

# ODsay 대중교통 경로 API (메인 경로 수집 소스)
# 발급: https://lab.odsay.com → 회원가입 → API 키 발급
ODSAY_API_KEY=YOUR_ODSAY_API_KEY_HERE

# TMAP 보행자 길찾기 API (보조 소스 — 계단/엘리베이터 상세 정보)
# 발급: https://openapi.sk.com → TMAP → 앱 등록
TMAP_API_KEY=YOUR_TMAP_API_KEY_HERE
```

#### 루트 .env 생성 (실제 키 입력 파일 — .gitignore 처리됨)

```env
# 카카오맵
VITE_KAKAO_MAP_KEY=실제키입력
KAKAO_REST_API_KEY=실제키입력

# Google Maps (선택)
GOOGLE_MAPS_API_KEY=실제키입력

# 네이버 지도 (선택)
NAVER_CLIENT_ID=실제키입력
NAVER_CLIENT_SECRET=실제키입력

# ODsay
ODSAY_API_KEY=실제키입력

# TMAP
TMAP_API_KEY=실제키입력
```

#### frontend/.env.example 생성 (없으면 새로 만들고, 있으면 아래 내용 추가)

```env
# 카카오맵 SDK (프론트엔드에서 지도 렌더링에 사용)
VITE_KAKAO_MAP_KEY=YOUR_KAKAO_MAP_APP_KEY_HERE

# 백엔드 URL
VITE_BACKEND_URL=http://localhost:8000
```

#### frontend/.env 생성 (실제 키 입력)

```env
VITE_KAKAO_MAP_KEY=실제키입력
VITE_BACKEND_URL=http://localhost:8000
```

#### backend/.env.example 생성 (없으면 새로 만들고, 있으면 아래 내용 추가)

```env
# 카카오 REST API (장소 검색, 주소-좌표 변환)
KAKAO_REST_API_KEY=YOUR_KAKAO_REST_API_KEY_HERE

# 네이버 지도 (선택)
NAVER_CLIENT_ID=YOUR_NAVER_CLIENT_ID_HERE
NAVER_CLIENT_SECRET=YOUR_NAVER_CLIENT_SECRET_HERE

# Google Maps (선택)
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# AI 서버 URL
AI_SERVER_URL=http://localhost:8001
```

#### backend/.env 생성 (실제 키 입력)

```env
KAKAO_REST_API_KEY=실제키입력
NAVER_CLIENT_ID=실제키입력
NAVER_CLIENT_SECRET=실제키입력
GOOGLE_MAPS_API_KEY=실제키입력
AI_SERVER_URL=http://localhost:8001
```

#### ai/.env.example 생성 (없으면 새로 만들고, 있으면 아래 내용 추가)

```env
# ODsay 대중교통 경로 API (메인)
ODSAY_API_KEY=YOUR_ODSAY_API_KEY_HERE

# TMAP 보행자 길찾기 API (보조)
TMAP_API_KEY=YOUR_TMAP_API_KEY_HERE
```

#### ai/.env 생성 (실제 키 입력)

```env
ODSAY_API_KEY=실제키입력
TMAP_API_KEY=실제키입력
```

---

## 4. 스마트쉘터 부대시설 파싱 (ai/preprocessing/parse_facilities.py)

```python
"""스마트버스쉘터 부대시설 텍스트 → 불리언 피처 파싱."""


def parse_facilities(text: str) -> dict:
    """
    '냉난방기, 공기청정기, 온열의자' 형태 텍스트를
    불리언 피처 딕셔너리로 변환한다.
    """
    text = str(text)
    return {
        "has_ac":           "냉난방" in text or "냉방" in text,
        "has_air_purifier": "공기청정" in text,
        "has_heated_seat":  "온열의자" in text or "냉온열의자" in text,
        "has_charger":      "충전기" in text,
        "has_wifi":         "와이파이" in text,
        "has_kiosk":        "키오스크" in text,
        "has_auto_door":    "자동문" in text,
    }
```

---

## 5. 데이터 레이어 구축 (ai/preprocessing/load_layers.py)

데이터팀 제공 기준 (부산 필터링 완료 + 위경도 포함 + 필요 컬럼만 정리된 상태).
이 모듈은 이상치 좌표 제거와 GeoDataFrame 변환만 담당한다.

```python
"""데이터 레이어 로딩 및 GeoDataFrame 변환 모듈."""
import pickle, warnings
from pathlib import Path
import geopandas as gpd
import pandas as pd
from shapely.geometry import Point

warnings.filterwarnings("ignore")

RAW_DIR   = Path("data/raw")
CACHE_DIR = Path("data/cache")
BUSAN_LAT = (34.8, 35.5)
BUSAN_LNG = (128.7, 129.4)


def _filter_busan(df, lat_col="위도", lng_col="경도"):
    return df[df[lat_col].between(*BUSAN_LAT) & df[lng_col].between(*BUSAN_LNG)].copy()

def _to_gdf(df, lat_col="위도", lng_col="경도"):
    geometry = [Point(row[lng_col], row[lat_col]) for _, row in df.iterrows()]
    return gpd.GeoDataFrame(df, geometry=geometry, crs="EPSG:4326")

def _read_csv(fname):
    for enc in ["utf-8-sig", "cp949", "euc-kr"]:
        try: return pd.read_csv(RAW_DIR / fname, encoding=enc)
        except: continue
    raise ValueError(f"파일 로딩 실패: {fname}")


def load_shelter():
    """무더위쉼터 + 한파쉼터 통합"""
    dfs = []
    for fname, stype in [
        ("부산광역시_무더위_쉼터_20251119.csv", "무더위"),
        ("부산광역시_한파_쉼터_현황_20251119.csv", "한파"),
    ]:
        df = _read_csv(fname); df["쉼터유형"] = stype
        dfs.append(df[["쉼터명", "위도", "경도", "쉼터유형"]])
    df_all = pd.concat(dfs, ignore_index=True).dropna(subset=["위도", "경도"])
    return _to_gdf(_filter_busan(df_all))

def load_cctv():
    """
    CCTV 두 파일 병합.
    두 파일은 좌표 기준 중복이 없는 별개 출처 → 단순 concat 후 이상치 제거.
    """
    rows = []
    df1 = _read_csv("부산광역시_방범용_CCTV_정보_20251229.csv")
    rows.append(df1[["위도","경도"]].assign(설치목적구분=df1["장비종류"], 카메라대수=1))
    df2 = _read_csv("CCTV정보_부산광역시.csv")
    rows.append(df2[["위도","경도","설치목적구분","카메라대수"]])
    df_all = pd.concat(rows, ignore_index=True).dropna(subset=["위도","경도"])
    return _to_gdf(_filter_busan(df_all))

def load_aed():
    df = pd.read_excel(RAW_DIR / "부산_AED_위경도_결과.xlsx")
    return _to_gdf(_filter_busan(df.dropna(subset=["위도","경도"])))

def load_wheelchair_charger():
    df = _read_csv("부산전동휠체어급속충전기표준데이터.csv")
    return _to_gdf(_filter_busan(df.dropna(subset=["위도","경도"])))

def load_dongbaekjeon():
    df = _read_csv("동백전_가맹점_현황.csv")
    return _to_gdf(_filter_busan(df.dropna(subset=["위도","경도"])))

def load_smart_shelter():
    from ai.preprocessing.parse_facilities import parse_facilities
    df = _read_csv("부산광역시_스마트_버스쉘터_설치_현황.csv")
    df = _filter_busan(df.dropna(subset=["위도","경도"]))
    fac = df["부대시설 "].apply(parse_facilities).apply(pd.Series)
    return _to_gdf(pd.concat([df[["정류소명","위도","경도"]], fac], axis=1))

def load_subway():
    df = pd.read_excel(RAW_DIR / "busan_subway_station_accessibility_processed.xlsx")
    df = _filter_busan(df.dropna(subset=["위도","경도"]))
    df["elevator_accessible"] = (df["엘리베이터"] == "O").astype(int)
    return _to_gdf(df[["역코드","역명","elevator_accessible","위도","경도"]])

def load_crosswalk():
    df = pd.read_excel(RAW_DIR / "busan_crosswalk_signal_shp_processed.xlsx")
    df = df.rename(columns={"lat":"위도","lng":"경도"})
    df = _filter_busan(df.dropna(subset=["위도","경도"]))
    df["has_signal"] = (df["light_yn"] == "유").astype(int)
    return _to_gdf(df[["gugun","has_signal","위도","경도"]])

def load_bus_stop():
    """⚠️ 전국 데이터 → 반드시 부산 필터링 (전국 22만행 → 부산 약 1만행)"""
    df = pd.read_excel(RAW_DIR / "bus_stop_national_csv_processed.xlsx")
    df = df[df["도시명"].str.contains("부산", na=False)]
    return _to_gdf(_filter_busan(df.dropna(subset=["위도","경도"])))


def load_all_layers(use_cache=True) -> dict:
    """
    모든 레이어 로딩. use_cache=True면 pickle 캐시 사용.
    데이터 파일 변경 시 use_cache=False로 재로딩.
    """
    CACHE_DIR.mkdir(parents=True, exist_ok=True)
    cache_path = CACHE_DIR / "all_layers.pkl"

    if use_cache and cache_path.exists():
        with open(cache_path, "rb") as f:
            return pickle.load(f)

    layers = {
        "shelter":            load_shelter(),
        "cctv":               load_cctv(),
        "aed":                load_aed(),
        "wheelchair_charger": load_wheelchair_charger(),
        "dongbaekjeon":       load_dongbaekjeon(),
        "smart_shelter":      load_smart_shelter(),
        "subway":             load_subway(),
        "crosswalk":          load_crosswalk(),
        "bus_stop":           load_bus_stop(),
    }

    with open(cache_path, "wb") as f:
        pickle.dump(layers, f)
    return layers
```

---

## 6. 피처 추출 (ai/features/extractor.py)

```python
"""
경로 좌표 시퀀스 → 피처 벡터 추출.

버퍼 기준:
- 50m  : CCTV, 횡단보도 (경로 바로 인접)
- 200m : 쉼터, AED, 충전기, 동백전 가맹점 (경유 가능 거리)
"""
from shapely.geometry import LineString

BUF_50M  = 0.00045
BUF_200M = 0.0018


def _count(gdf, buf):
    if gdf is None or len(gdf) == 0: return 0
    return gdf[gdf.geometry.within(buf)].shape[0]

def _any(gdf, buf):
    return int(_count(gdf, buf) > 0)

def _mean_col(gdf, buf, col):
    if gdf is None or len(gdf) == 0: return 1.0
    nearby = gdf[gdf.geometry.within(buf)]
    return float(nearby[col].mean()) if len(nearby) > 0 else 1.0

def _any_col(gdf, buf, col):
    if gdf is None or len(gdf) == 0: return 0
    nearby = gdf[gdf.geometry.within(buf)]
    return int(nearby[col].any()) if len(nearby) > 0 else 0


def extract_route_features(route_coords: list, data_layers: dict) -> dict:
    """
    경로 1개의 피처 벡터를 계산하여 반환한다.

    Parameters
    ----------
    route_coords : list of (lat, lng) tuples — 최소 2개 필요
    data_layers  : load_all_layers() 반환값
    """
    if len(route_coords) < 2:
        return _zero_features()

    line         = LineString([(lng, lat) for lat, lng in route_coords])
    buf_50m      = line.buffer(BUF_50M)
    buf_200m     = line.buffer(BUF_200M)
    route_len_km = max(line.length * 111.0, 0.1)

    return {
        "cctv_density_50m":              round(_count(data_layers.get("cctv"), buf_50m) / route_len_km, 4),
        "crosswalk_count":               _count(data_layers.get("crosswalk"), buf_50m),
        "crosswalk_signal_ratio":        _mean_col(data_layers.get("crosswalk"), buf_50m, "has_signal"),
        "shelter_nearby":                _any(data_layers.get("shelter"), buf_200m),
        "aed_nearby":                    _any(data_layers.get("aed"), buf_200m),
        "wheelchair_charger_nearby":     _any(data_layers.get("wheelchair_charger"), buf_200m),
        "smart_shelter_nearby":          _any(data_layers.get("smart_shelter"), buf_200m),
        "smart_shelter_has_ac":          _any_col(data_layers.get("smart_shelter"), buf_200m, "has_ac"),
        "dongbaekjeon_store_count_200m": _count(data_layers.get("dongbaekjeon"), buf_200m),
        "bus_stop_count_200m":           _count(data_layers.get("bus_stop"), buf_200m),
    }


def _zero_features() -> dict:
    return {k: 0 for k in [
        "cctv_density_50m", "crosswalk_count", "crosswalk_signal_ratio",
        "shelter_nearby", "aed_nearby", "wheelchair_charger_nearby",
        "smart_shelter_nearby", "smart_shelter_has_ac",
        "dongbaekjeon_store_count_200m", "bus_stop_count_200m",
    ]}
```

---

## 7. XGBoost 스코어링 (ai/scoring/)

### 7-1. 피처 컬럼 + Y라벨 + 학습 (train.py)

```python
"""
XGBRanker 학습 모듈.

Y라벨 전략:
  현재: 규칙 기반 auto_label() 함수로 자동 생성
  추후: 조용빈 선임님 샘플 경로 평가로 검증
  최종: 실제 사용자 선택 데이터로 재학습
"""
import pickle
from pathlib import Path
import numpy as np
import pandas as pd
from sklearn.model_selection import GroupKFold
from xgboost import XGBRanker

PROFILES  = ["general", "elderly", "child", "disabled"]
MODEL_DIR = Path("data/cache")

FEATURE_COLS = [
    # 경사·지형 (ODsay 응답 + DEM 기반)
    "avg_slope_percent", "max_slope_percent", "min_slope_percent", "slope_iqr",
    # 이동·접근성
    "stair_count", "elevator_ratio", "transfer_count",
    "walk_distance_m", "total_duration_min", "is_low_floor_bus",
    # 안전
    "cctv_density_50m", "crosswalk_count", "crosswalk_signal_ratio",
    # 편의시설
    "shelter_nearby", "aed_nearby", "wheelchair_charger_nearby",
    "smart_shelter_nearby", "smart_shelter_has_ac",
    # 부산 특화
    "dongbaekjeon_store_count_200m", "bus_stop_count_200m",
    # 환경 (수동 입력)
    "crowd_level", "weather_risk",
]


def auto_label(features: dict, profile: str) -> float:
    """
    규칙 기반 Y라벨 자동 생성 (0~1).
    ⚠️ 실제 사용자 선택 데이터 수집 후 교체 대상.
    """
    score = 1.0
    if profile == "general":
        score -= features.get("avg_slope_percent", 0) * 0.02
        score -= features.get("crosswalk_count", 0) * 0.01
    elif profile == "elderly":
        score -= features.get("stair_count", 0) * 0.15
        score -= features.get("avg_slope_percent", 0) * 0.08
        score -= features.get("max_slope_percent", 0) * 0.05
        score -= features.get("transfer_count", 0) * 0.10
        score += features.get("elevator_ratio", 0) * 0.20
        score += features.get("shelter_nearby", 0) * 0.05
    elif profile == "child":
        score -= features.get("crosswalk_count", 0) * 0.08
        score += features.get("crosswalk_signal_ratio", 0) * 0.15
    elif profile == "disabled":
        score -= features.get("stair_count", 0) * 0.25
        score += features.get("elevator_ratio", 0) * 0.30
        score += features.get("is_low_floor_bus", 0) * 0.15
        score += features.get("wheelchair_charger_nearby", 0) * 0.05
    return max(0.0, min(1.0, score))


def generate_synthetic_data(n_groups=300, n_routes=3, seed=42) -> pd.DataFrame:
    """
    가상 경로 데이터 생성.
    ⚠️ 실데이터 수신 후 이 함수를 실제 데이터 로딩으로 교체한다.
    """
    np.random.seed(seed)
    records = []
    for g in range(n_groups):
        for _ in range(n_routes):
            slopes = np.random.uniform(0, 20, 10)
            rec = {
                "group_id": g,
                "avg_slope_percent": float(slopes.mean()),
                "max_slope_percent": float(slopes.max()),
                "min_slope_percent": float(np.random.uniform(-10, 0)),
                "slope_iqr":         float(np.percentile(slopes, 75) - np.percentile(slopes, 25)),
                "stair_count":       int(np.random.randint(0, 10)),
                "elevator_ratio":    float(np.random.uniform(0, 1)),
                "transfer_count":    int(np.random.randint(0, 3)),
                "walk_distance_m":   float(np.random.uniform(100, 2000)),
                "total_duration_min":float(np.random.uniform(5, 60)),
                "is_low_floor_bus":  int(np.random.choice([0, 1])),
                "cctv_density_50m":  float(np.random.uniform(0, 5)),
                "crosswalk_count":   int(np.random.randint(0, 10)),
                "crosswalk_signal_ratio": float(np.random.uniform(0, 1)),
                "shelter_nearby":    int(np.random.randint(0, 2)),
                "aed_nearby":        int(np.random.randint(0, 2)),
                "wheelchair_charger_nearby": int(np.random.randint(0, 2)),
                "smart_shelter_nearby": int(np.random.randint(0, 2)),
                "smart_shelter_has_ac": int(np.random.randint(0, 2)),
                "dongbaekjeon_store_count_200m": int(np.random.randint(0, 30)),
                "bus_stop_count_200m": int(np.random.randint(0, 8)),
                "crowd_level":  float(np.random.uniform(0, 1)),
                "weather_risk": float(np.random.uniform(0, 30)),
            }
            for profile in PROFILES:
                rec[f"label_{profile}"] = auto_label(rec, profile)
            records.append(rec)
    return pd.DataFrame(records)


def train_rankers(df=None) -> dict:
    """프로필별 XGBRanker GroupKFold 학습."""
    if df is None:
        print("가상 데이터로 학습 (실데이터 수신 후 교체 필요)")
        df = generate_synthetic_data()

    rankers = {}
    gkf = GroupKFold(n_splits=5)

    for profile in PROFILES:
        X = df[FEATURE_COLS]
        y = df[f"label_{profile}"]
        ranker = XGBRanker(
            objective="rank:pairwise",
            max_depth=6, learning_rate=0.05, n_estimators=300,
            subsample=0.8, colsample_bytree=0.8,
            tree_method="hist", random_state=42,
        )
        for tr_idx, val_idx in gkf.split(X, y, groups=df["group_id"]):
            g_tr  = df.iloc[tr_idx].groupby("group_id").size().values
            g_val = df.iloc[val_idx].groupby("group_id").size().values
            ranker.fit(X.iloc[tr_idx], y.iloc[tr_idx], group=g_tr,
                       eval_set=[(X.iloc[val_idx], y.iloc[val_idx])],
                       eval_group=[g_val], verbose=False)
        rankers[profile] = ranker

    MODEL_DIR.mkdir(parents=True, exist_ok=True)
    with open(MODEL_DIR / "rankers.pkl", "wb") as f:
        pickle.dump(rankers, f)
    return rankers


def load_rankers() -> dict:
    p = MODEL_DIR / "rankers.pkl"
    if not p.exists():
        return train_rankers()
    with open(p, "rb") as f:
        return pickle.load(f)
```

### 7-2. 추론 (ai/scoring/predict.py)

```python
"""
추론 모듈: XGB → 로짓 패널티 → Softmax → Top-K.

로짓 패널티 설계 원칙:
  양수 값 : 해당 피처가 클수록 불리 (감산)
  음수 값 : 해당 피처가 클수록 유리 (가산)
  베이스라인 고정 상수 → 추후 날씨·혼잡도에 따른 동적 조정으로 확장
"""
import numpy as np
import pandas as pd
from ai.scoring.train import FEATURE_COLS

LOGIT_PENALTIES = {
    "general": {
        "avg_slope_percent": 0.2, "crowd_level": 0.3, "weather_risk": 0.3,
    },
    "elderly": {
        "stair_count": 2.0, "avg_slope_percent": 1.5, "max_slope_percent": 1.0,
        "transfer_count": 1.2, "walk_distance_m": 0.001, "crowd_level": 0.8,
        "weather_risk": 1.0,
        "elevator_ratio": -1.5,       # 가산
        "shelter_nearby": -0.8,       # 가산
        "smart_shelter_has_ac": -0.6, # 가산
    },
    "child": {
        "stair_count": 0.5, "crosswalk_count": 0.8, "crowd_level": 0.5,
        "crosswalk_signal_ratio": -1.0,  # 가산
    },
    "disabled": {
        "stair_count": 3.0, "avg_slope_percent": 2.0, "max_slope_percent": 1.5,
        "walk_distance_m": 0.0008,
        "elevator_ratio": -2.0,              # 가산
        "is_low_floor_bus": -1.5,            # 가산
        "wheelchair_charger_nearby": -0.5,   # 가산
    },
}


def predict_and_rank(rankers: dict, route_features_list: list,
                     profile: str, top_k=3) -> list:
    """XGB 점수 → 로짓 패널티 → Softmax → Top-K 반환."""
    ranker = rankers[profile]
    X = pd.DataFrame([{col: f.get(col, 0) for col in FEATURE_COLS}
                      for f in route_features_list])

    xgb_scores = ranker.predict(X)
    adjusted   = xgb_scores.astype(float).copy()

    for i, feat in enumerate(route_features_list):
        for fname, w in LOGIT_PENALTIES.get(profile, {}).items():
            adjusted[i] -= w * feat.get(fname, 0)

    e     = np.exp(adjusted - adjusted.max())
    probs = e / e.sum()
    ranked = np.argsort(probs)[::-1][:top_k]

    return [{
        "rank":           r + 1,
        "route_index":    int(idx),
        "xgb_score":      round(float(xgb_scores[idx]), 4),
        "adjusted_score": round(float(adjusted[idx]), 4),
        "probability":    round(float(probs[idx]), 4),
    } for r, idx in enumerate(ranked)]
```

### 7-3. SHAP 추천 이유 (ai/scoring/explain.py)

```python
"""SHAP 기반 추천 이유 자동 생성."""
import shap, pandas as pd
from ai.scoring.train import FEATURE_COLS

REASON_MAP = {
    "elevator_ratio":              ("승강기로 이동할 수 있어 계단을 피할 수 있어요", "승강기 접근이 어려운 구간이 있어요"),
    "stair_count":                 ("계단이 없는 편이에요", "계단이 많아 이동이 불편할 수 있어요"),
    "avg_slope_percent":           ("평지 위주의 경로예요", "경사 구간이 있어요"),
    "max_slope_percent":           ("가장 가파른 구간도 완만한 편이에요", "급경사 구간이 포함돼 있어요"),
    "transfer_count":              ("환승 없이 한 번에 이동해요", "환승이 있는 경로예요"),
    "crowd_level":                 ("혼잡하지 않은 경로예요", "혼잡한 구간이 포함돼 있어요"),
    "is_low_floor_bus":            ("저상버스가 포함된 경로예요", "저상버스가 없는 경로예요"),
    "crosswalk_signal_ratio":      ("신호등 있는 횡단보도 위주예요", "신호등 없는 횡단보도가 포함돼 있어요"),
    "shelter_nearby":              ("경로 근처에 쉼터가 있어요", None),
    "aed_nearby":                  ("경로 근처에 AED가 있어요", None),
    "wheelchair_charger_nearby":   ("경로 근처에 전동휠체어 충전기가 있어요", None),
    "smart_shelter_has_ac":        ("냉난방 가능한 버스쉘터가 근처에 있어요", None),
    "cctv_density_50m":            ("CCTV가 잘 설치된 안전한 경로예요", "CCTV가 적은 구간이 있어요"),
    "dongbaekjeon_store_count_200m":("동백전 가맹점이 많은 구역을 지나요", None),
}


def generate_reasons(ranker, X_route: pd.DataFrame, top_n=4) -> list:
    explainer   = shap.TreeExplainer(ranker)
    shap_values = explainer.shap_values(X_route)
    top_feats   = pd.Series(shap_values[0], index=X_route.columns).abs().nlargest(top_n).index

    reasons = []
    for feat in top_feats:
        if feat not in REASON_MAP: continue
        pos, neg = REASON_MAP[feat]
        shap_val = shap_values[0][X_route.columns.get_loc(feat)]
        msg = pos if shap_val > 0 else neg
        if msg: reasons.append(msg)
    return reasons
```

---

## 8. 강화학습 모듈 (ai/rl/)

### 8-1. 환경 (ai/rl/environment.py)

```python
"""
강화학습 환경.

State  : 경로 후보들의 피처 벡터 (flatten)
Action : 경로 후보 중 하나 선택 (0, 1, 2, ...)
Reward : 선택한 경로의 XGBoost 점수 (0~1 정규화)

XGBoost가 경로마다 점수를 매기고,
RL 에이전트가 그 점수를 보상으로 받아 최적 선택 정책을 학습한다.
"""
import numpy as np
import pandas as pd
from ai.scoring.train import FEATURE_COLS


class RouteSelectionEnv:

    def __init__(self, rankers: dict, profile: str):
        self.rankers = rankers
        self.profile = profile
        self.routes  = []

    @property
    def n_actions(self):
        return len(self.routes)

    @property
    def state_dim(self):
        return len(FEATURE_COLS) * max(self.n_actions, 1)

    def reset(self, route_features_list: list) -> np.ndarray:
        self.routes = route_features_list
        return self._state()

    def step(self, action: int) -> tuple:
        """선택한 경로의 XGBoost 점수를 Reward로 반환."""
        selected = self.routes[action]
        X = pd.DataFrame([{col: selected.get(col, 0) for col in FEATURE_COLS}])
        xgb_score = float(self.rankers[self.profile].predict(X)[0])
        reward = np.clip(xgb_score, 0.0, 1.0)
        return self._state(), reward, True  # done=True (1스텝 종료)

    def _state(self) -> np.ndarray:
        vecs = [[f.get(col, 0) for col in FEATURE_COLS] for f in self.routes]
        return np.array(vecs, dtype=np.float32).flatten()
```

### 8-2. DQN 에이전트 (ai/rl/agent.py)

```python
"""
DQN 에이전트.

팀 확정 하이퍼파라미터:
  활성화 함수 : SiLU (Swish)
  최적화      : Adam lr=0.001
  손실 함수   : MSE (Bellman 오차)
  할인율(γ)  : 0.95
"""
import random
from collections import deque
import numpy as np
import torch
import torch.nn as nn


class QNetwork(nn.Module):
    def __init__(self, state_dim: int, action_dim: int):
        super().__init__()
        self.net = nn.Sequential(
            nn.Linear(state_dim, 128), nn.SiLU(),
            nn.Linear(128, 128),       nn.SiLU(),
            nn.Linear(128, action_dim),
        )

    def forward(self, x):
        return self.net(x)


class DQNAgent:

    def __init__(self, state_dim: int, action_dim: int,
                 lr=0.001, gamma=0.95, epsilon=1.0,
                 epsilon_min=0.01, epsilon_decay=0.995, buffer_size=10000):
        self.action_dim    = action_dim
        self.gamma         = gamma
        self.epsilon       = epsilon
        self.epsilon_min   = epsilon_min
        self.epsilon_decay = epsilon_decay

        self.q_net      = QNetwork(state_dim, action_dim)
        self.target_net = QNetwork(state_dim, action_dim)
        self.target_net.load_state_dict(self.q_net.state_dict())

        self.optimizer = torch.optim.Adam(self.q_net.parameters(), lr=lr)
        self.loss_fn   = nn.MSELoss()
        self.buffer    = deque(maxlen=buffer_size)

    def act(self, state: np.ndarray) -> int:
        """epsilon-greedy 행동 선택."""
        if random.random() < self.epsilon:
            return random.randint(0, self.action_dim - 1)
        with torch.no_grad():
            return int(self.q_net(torch.FloatTensor(state)).argmax())

    def store(self, state, action, reward, next_state, done):
        self.buffer.append((state, action, reward, next_state, done))

    def train_step(self, batch_size=32):
        if len(self.buffer) < batch_size:
            return None
        batch = random.sample(self.buffer, batch_size)
        s, a, r, ns, d = zip(*batch)

        s  = torch.FloatTensor(np.array(s))
        a  = torch.LongTensor(a)
        r  = torch.FloatTensor(r)
        ns = torch.FloatTensor(np.array(ns))
        d  = torch.FloatTensor(d)

        q_curr = self.q_net(s).gather(1, a.unsqueeze(1)).squeeze()
        q_tgt  = r + self.gamma * self.target_net(ns).max(1)[0] * (1 - d)

        loss = self.loss_fn(q_curr, q_tgt.detach())
        self.optimizer.zero_grad(); loss.backward(); self.optimizer.step()

        self.epsilon = max(self.epsilon_min, self.epsilon * self.epsilon_decay)
        return float(loss)

    def sync_target(self):
        self.target_net.load_state_dict(self.q_net.state_dict())
```

---

## 9. .gitignore 추가 항목

기존 .gitignore 파일에 아래 항목을 추가하세요.
없으면 루트에 새로 생성하세요.

```
# ============================================================
# API 키 파일 — 절대 커밋 금지
# ============================================================
.env
.env.local
.env.*.local
frontend/.env
backend/.env
ai/.env

# ============================================================
# 데이터 캐시 및 원본
# ============================================================
data/cache/
data/raw/

# 모델 파일
data/cache/rankers.pkl
data/cache/busanjin_walk.graphml
data/cache/all_layers.pkl
```

> .env.example 파일은 커밋해도 됩니다 (실제 키 값이 없으므로).
> .env 파일은 절대 커밋하지 마세요.

---

## 10. ai/requirements.txt

```
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
httpx==0.27.0
python-dotenv==1.0.1
pandas==2.2.2
geopandas==1.0.1
shapely==2.0.6
numpy==1.26.4
xgboost==2.1.1
scikit-learn==1.5.1
shap==0.46.0
openpyxl==3.1.5
torch==2.3.0
osmnx==1.9.4
networkx==3.3
```

---

## 11. 작업 순서

1. 현재 레포 전체 파악 — 기존 파일 전부 읽기
2. **.gitignore 업데이트** — .env 파일 및 data/cache/, data/raw/ 추가
3. **.env.example 파일 생성** — 루트 / frontend / backend / ai 각각 생성
4. **.env 파일 생성** — 루트 / frontend / backend / ai 각각 생성 (실제 키 입력 자리만 만들어두고 키는 비워둘 것 — 팀원이 직접 입력)
5. data/ 폴더 정리 — 원본 파일 data/raw/ 로 이동, data/cache/ 생성
6. pathfinding/ 코드 확인 후 삭제 (ai/ 하위로 이동 필요한 것 있으면 먼저 이동)
7. KSP/ACO/GA/Tree 관련 코드 및 import 전체 제거
8. ai/collectors/ 구현 — base.py → odsay_collector.py → tmap_collector.py → osmnx_collector.py
9. ai/preprocessing/ 구현 — parse_facilities.py → load_layers.py
10. ai/features/ 구현 — extractor.py
11. ai/scoring/ 구현 — train.py → predict.py → explain.py
12. ai/rl/ 구현 — environment.py → agent.py
13. ai/tests/ 작성 및 pytest ai/tests/ -v 전체 통과

---

## 12. 최종 확인 체크리스트

- [ ] 상위 폴더가 backend / frontend / data / docs / ai 5개 외 없음
- [ ] pathfinding/ 폴더 및 KSP/ACO/GA/Tree 관련 코드 전부 삭제됨
- [ ] data/ 폴더 열면 raw/ cache/ processed/ 만 보임
- [ ] 원본 CSV/XLSX 파일이 전부 data/raw/ 에 있음
- [ ] .env.example 파일이 루트 / frontend / backend / ai 4곳 모두 생성됨
- [ ] .env 파일이 루트 / frontend / backend / ai 4곳 모두 생성됨 (키 입력 자리만, 실제 키는 비워둠)
- [ ] .env 파일이 .gitignore 에 등록됨 (절대 커밋되지 않도록)
- [ ] .env.example 파일은 .gitignore 에 없음 (커밋 가능해야 함)
- [ ] ODsay가 메인 경로 수집 소스로 동작
- [ ] OSMnx가 fallback으로 항상 최소 1개 경로 보장
- [ ] pytest ai/tests/ -v 전체 통과
- [ ] data/cache/ 및 data/raw/ 가 .gitignore 에 등록됨
- [ ] 모든 코드에 한국어 docstring 작성 완료
