[project]
name = "barrier-free-route-backend"
version = "2.0.0"
description = "부산 전역 교통약자 접근성 경로 추천 API"
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = [".", ".."]
testpaths = ["tests"]
