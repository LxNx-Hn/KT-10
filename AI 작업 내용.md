# AI 구현 인수인계

최신 제품 기준은 `docs/PRODUCT_DECISIONS.md`입니다.

## 확정 구조

AI 추천을 두 단계로 분리합니다.

1. **경로 사실·특성 라벨링**: 공급자 경로에서 확인된 시간, 도보, 환승,
   계단·승강기·저상버스, GLO-90 지형 추정, 건물 그늘을 구조화하고 사실
   배지를 생성합니다. 미확인은 0이나 없음으로 만들지 않습니다.
2. **프로필·조건 적합도 및 순위**: 일반·고령자·아동·청소년·장애인·
   임산부 6개 기본 프로필과 짐 많음·유아차·계단 회피·그늘 우선·
   저상버스 우선·환승 최소 조건을 경로 사실과 매칭해 점수순으로
   정렬합니다.

LLM/Codex judge 라벨은 실제 사용자 라벨이 적은 초기 베이스라인으로
허용합니다. 공급자와 기존 순위를 가린 동결 피처를 평가하고 루브릭,
프롬프트 버전, 판단 근거를 보존해야 합니다. 산출물은
`rankers.judge-baseline.pkl`이며 사람 라벨 후보나 승인 운영 모델과
구분합니다.

## 완료

- ODsay `searchPubTransPathT` + `loadLane`, TMAP 보행, 부산 전역 동적 OSMnx 경로 수집
- 공급자별 명시적 오류와 geometry 정확도(`exact/mixed/estimated`)
- EPSG:5179 공간 버퍼 피처와 Open-Meteo Copernicus GLO-90 지형 피처
- 사고정보 제거, 결측값 `None` 유지, 교통카드 전 혼잡 추정 금지
- 사람 라벨용 XGBRanker, 초기 라벨링 후보 생성 API/CLI
- 짐·계단·저상 우선·이동지원 조건의 학습 가능한 상호작용 피처
- 서버 서명 후기 snapshot, 로그인 사용자 온라인 개인화, 동의 후기 후보 재학습
- 모델 미준비 시 `/recommend` 503, `/model/status`로 상태 확인

## 운영 전 필수

1. ODsay Server Key 애플리케이션에 현재 개발 공인 IP
   `119.202.222.84` 등록 후 라이브 smoke test
2. 6개 프로필과 6개 상황 조건을 FE·BE·AI에서 같은 계약으로 통일
3. 그늘을 포함한 동결 경로 사실·특성 스키마와 부산 층화 OD 배치 생성
4. 블라인드 judge 평가, 일관성 검사, OD holdout 비교를 거쳐
   `rankers.judge-baseline.pkl` 생성
5. 실제 사용자·전문가 라벨은 별도 후보로 학습하고 관리자 승인 전에는
   `rankers.pkl`을 교체하지 않음
6. 실제 공급자 smoke test와 `data/catalog.json` 출처·라이선스 확인

PPO 강화학습은 사용하지 않습니다. 현재 후기량과 안전성 요구에서는 서명된 명시적 피드백을 이용한 온라인 로지스틱 개인화와 통제된 XGBRanker 재학습이 구현되어 있습니다. RL 소스는 연구 실험으로만 남고 운영 의존성에 포함되지 않습니다.

개인화는 후기 5건까지 베이스라인을 우선하고 최대 35%만 반영합니다.
동의 후기를 이용한 전역 후보 학습과 운영 승격은 관리자가 수동으로
수행합니다.
